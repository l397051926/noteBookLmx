@JSONField 格式化输出时候使用
 @JSONField(jsonDirect = true)   // 如果数据是string 会进一步格式化json格式
 @JSONField(format = "yyyy-MM-ddThh:mm:ss:mmm")  //格式化日期使用，在转成字符串时候

@JsonInclude(JsonInclude.Include.NON_NULL)
    格式化的时候去掉 null 的数据
